<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Redirect to index.html for non-existing paths
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
